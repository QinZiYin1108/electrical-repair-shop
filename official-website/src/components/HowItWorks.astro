<section class="section" style="background:var(--color-bg-alt);">
  <div class="container">
    <h2 class="section-title">三步搞定维修</h2>
    <p class="section-subtitle">简单快捷，全程线上操作</p>

    <div class="steps">
      <div class="step">
        <div class="step-number">1</div>
        <h3>在线预约</h3>
        <p>打开安修到家小程序，选择维修品类，填写故障描述和地址信息，一键提交预约。</p>
      </div>
      <div class="step-arrow">→</div>
      <div class="step">
        <div class="step-number">2</div>
        <h3>师傅上门</h3>
        <p>系统自动匹配附近专业师傅，师傅接单后主动联系确认时间，准时上门检修。</p>
      </div>
      <div class="step-arrow">→</div>
      <div class="step">
        <div class="step-number">3</div>
        <h3>维修完成</h3>
        <p>维修完成后在线支付，对服务进行评价。享受质保服务，有问题随时申请售后。</p>
      </div>
    </div>
  </div>
</section>

<style>
.steps {
  display: flex;
  align-items: flex-start;
  justify-content: center;
  gap: 20px;
}

.step {
  flex: 1;
  max-width: 300px;
  text-align: center;
}

.step-number {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--color-primary);
  color: #fff;
  font-size: 1.5rem;
  font-weight: 800;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px;
  box-shadow: 0 8px 24px rgba(37,99,235,.25);
}

.step h3 {
  font-size: 1.1rem;
  margin-bottom: 8px;
}

.step p {
  font-size: .92rem;
  color: var(--color-text-secondary);
  line-height: 1.7;
}

.step-arrow {
  font-size: 1.5rem;
  color: var(--color-border);
  margin-top: 16px;
  flex-shrink: 0;
}

@media (max-width: 768px) {
  .steps {
    flex-direction: column;
    align-items: center;
  }
  .step { max-width: 100%; }
  .step-arrow { transform: rotate(90deg); margin: 4px 0; }
}
</style>
