<section class="hero">
  <div class="hero-bg"></div>
  <div class="container hero-content">
    <div class="hero-text">
      <span class="hero-badge">🔧 全品类家电维修</span>
      <h1>专业电器维修<br /><span class="hero-highlight">一键预约上门</span></h1>
      <p>空调、冰箱、洗衣机、电视、热水器…… 覆盖全品类家电，专业认证师傅，透明定价，品质保障，最快 2 小时上门。</p>
      <div class="hero-actions">
        <a href="/contact" class="btn btn-primary" style="padding:16px 40px;font-size:1.05rem;">
          立即预约维修 →
        </a>
        <a href="/services" class="btn btn-outline" style="padding:16px 40px;font-size:1.05rem;">
          查看服务项目
        </a>
      </div>
      <div class="hero-stats">
        <div class="stat">
          <strong>10000+</strong>
          <span>服务用户</span>
        </div>
        <div class="stat">
          <strong>500+</strong>
          <span>认证师傅</span>
        </div>
        <div class="stat">
          <strong>98%</strong>
          <span>好评率</span>
        </div>
      </div>
    </div>
    <div class="hero-image">
      <img
        src="/images/logo.png"
        alt="安修到家"
        class="hero-logo"
        width="280"
        height="280"
        loading="eager"
      />
    </div>
  </div>
</section>

<style>
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  overflow: hidden;
  padding-top: 72px;
}

.hero-bg {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 80% 60% at 20% 50%, rgba(37,99,235,.08), transparent),
    radial-gradient(ellipse 60% 70% at 80% 40%, rgba(245,158,11,.06), transparent),
    linear-gradient(180deg, #F8FAFC 0%, #fff 100%);
  z-index: 0;
}

.hero-content {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 64px;
  align-items: center;
}

.hero-badge {
  display: inline-block;
  background: var(--color-primary-light);
  color: var(--color-primary);
  padding: 6px 16px;
  border-radius: 20px;
  font-size: .9rem;
  font-weight: 600;
  margin-bottom: 24px;
}

.hero-text h1 {
  font-size: 3rem;
  font-weight: 800;
  line-height: 1.25;
  color: var(--color-text);
  margin-bottom: 20px;
}

.hero-highlight {
  background: linear-gradient(135deg, var(--color-primary), #6366F1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero-text p {
  font-size: 1.1rem;
  color: var(--color-text-secondary);
  line-height: 1.8;
  margin-bottom: 32px;
  max-width: 480px;
}

.hero-actions {
  display: flex;
  gap: 16px;
  margin-bottom: 48px;
  flex-wrap: wrap;
}

.hero-stats {
  display: flex;
  gap: 40px;
}

.stat strong {
  display: block;
  font-size: 1.75rem;
  font-weight: 800;
  color: var(--color-primary);
}

.stat span {
  font-size: .9rem;
  color: var(--color-text-secondary);
}

.hero-image {
  display: flex;
  justify-content: center;
  align-items: center;
}

.hero-logo {
  border-radius: 24px;
  box-shadow: 0 24px 80px rgba(37,99,235,.18);
  animation: float 6s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-18px); }
}

@media (max-width: 768px) {
  .hero-content {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 40px;
  }
  .hero-text h1 { font-size: 2rem; }
  .hero-text p { max-width: 100%; }
  .hero-actions { justify-content: center; }
  .hero-stats { justify-content: center; }
  .hero-logo { width: 180px; height: 180px; }
}
</style>
